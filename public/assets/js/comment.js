// Used the following for documentation: https://developer.mozilla.org/en-US/docs/Web/API/HTMLFormElement/formdata_event
// Still needs to be tested and linked to `/api/posts`
async function newFormHandler(event) {
    event.preventDefault();
    
}
document.querySelector('').addEventListener('submit', newFormHandler);