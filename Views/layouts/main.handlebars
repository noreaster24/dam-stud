{{!-- This is the template for the main page --}}
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>What's Up Dog?!</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <div class="wrapper">
        <header>
            <h1>
                <a href="/">What's Up Dog?!</a>
            </h1>
            
            <nav>
                {{#if loggedIn}}
                <button id="logout" class="btn-no-style">logout</button>
                {{else}}
                <a href="/login">login</a>
                {{/if}}
            </nav>
        </header>
        <main>
            <div class="program-description">
                <p>Avoid the spam and stop using Craigslist.  What's Up Dog is the premier spot to find fellow dog enthusiasts!</p>
            </div>
            {{!-- Takes user to create their new profile
            <div class="generate-profile">
                <button type="button" class="btn btn-primary">New Profile</button>
                <h4 class="text-light bg-dark p-3">Create a profile introducing yourself to the group!</h4>
            </div> --}}
            {{!-- Loads all user information from homepage.handlebars --}}
            {{{ body }}}
        </main>
        <footer>
            Thanks for visiting!
        </footer>
    </div>

    <script src="/js/logout.js"></script>
</body>

</html>